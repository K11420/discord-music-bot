# 🎮 Minecraft Bedrock Server Website

プロフェッショナルなマイクラサーバー管理Webサイト - リアルタイム監視機能付き

## ✨ 機能

### 📱 公開サイト (https://minecraft.schale41.jp)
- 🎨 美しいレスポンシブデザイン
- 📊 **リアルタイムサーバー状態表示**
- ⚡ WebSocket接続による即座の更新（3秒ごと）
- 🎯 サーバー参加ガイド
- 📋 ワンクリックIPコピー機能
- 🌟 パーティクルアニメーション背景

### 🔐 管理パネル (https://minecraft.schale41.jp/admin)
- 👨‍💼 管理者認証システム
- 📊 **リアルタイム監視ダッシュボード**
  - プロセス状態（bedrock_server）
  - Screenセッション状態（tama）
  - ポート状態（4096）
  - オンライン/オフライン自動検出
- 🎮 サーバー制御
  - ワンクリック起動/停止/再起動
  - Screen セッション自動管理
- ⚡ クイックコマンド
  - プレイヤーリスト
  - ワールド保存
  - 時間/天気変更
  - 難易度変更
- 💻 リアルタイムターミナル
  - コマンド送信
  - ログ表示
- 📜 サーバーログビューア

## 🚀 リアルタイム監視の仕組み

### サーバー状態チェック
```javascript
// 3つの項目を同時チェック
1. プロセス確認: ps aux | grep bedrock_server
2. Screen確認: screen -ls | grep tama
3. ポート確認: lsof -i :4096

// すべてがOKの場合のみ「オンライン」と判定
online = processRunning && screenRunning && portOpen
```

### WebSocket通信
- **更新頻度**: 3秒ごと
- **自動再接続**: 接続切断時に自動で再接続
- **双方向通信**: サーバー→クライアントの状態プッシュ

## 🔧 設定

### サーバー情報
```
Screen Session: tama
Server Path: ~/tama/bedrock-server-1.21.114.1
Port: 4096 (UDP)
Address: schale41.jp
```

### 管理者認証
```
Default Password: admin123
URL: https://minecraft.schale41.jp/admin
```

### パスワード変更
```bash
cd /home/kbt0/webapp/minecraft-site
ADMIN_PASSWORD=your_secure_password node server.js
```

## 📊 API エンドポイント

### GET /api/status
サーバー状態を取得（リアルタイム）

レスポンス:
```json
{
  "online": true,
  "processRunning": true,
  "screenRunning": true,
  "portOpen": true,
  "players": { "online": 0, "max": 10 },
  "uptime": 3600,
  "address": "schale41.jp",
  "port": 4096
}
```

### POST /api/server/start (要認証)
サーバーを起動

### POST /api/server/stop (要認証)
サーバーを停止

### POST /api/server/command (要認証)
コマンドを送信
```json
{ "command": "list" }
```

## 🌐 WebSocket

### 接続
```javascript
ws://localhost:3000 (HTTP)
wss://minecraft.schale41.jp (HTTPS)
```

### メッセージタイプ
```javascript
// 状態更新（3秒ごと）
{
  "type": "status_update",
  "data": {
    "online": true,
    "processRunning": true,
    "screenRunning": true,
    "portOpen": true,
    "players": { "online": 0, "max": 10 },
    "uptime": 3600
  }
}

// ログ更新
{
  "type": "logs",
  "data": "サーバーログ内容..."
}
```

## 🛠️ 起動方法

### 開発環境
```bash
cd /home/kbt0/webapp/minecraft-site
npm install
npm start
```

### 本番環境（現在の設定）
```bash
PORT=3000 ADMIN_PASSWORD=admin123 node server.js
```

### Cloudflare Tunnel
```bash
cd /home/kbt0/webapp
./cloudflared tunnel run face-ai
```

## 📁 ファイル構成

```
minecraft-site/
├── index.html              # メインページ
├── admin.html              # 管理パネル
├── server.js               # Node.jsサーバー（WebSocket対応）
├── package.json
├── css/
│   ├── style.css          # メインスタイル
│   └── admin.css          # 管理画面スタイル
├── js/
│   ├── main.js            # メインJS（WebSocket接続）
│   ├── admin.js           # 管理画面JS（リアルタイム更新）
│   └── particles-config.js
└── api/
    └── status.json
```

## 🔍 トラブルシューティング

### サーバーが起動しない
```bash
# Screen セッション確認
screen -ls

# セッションに接続
screen -r tama

# サーバーパス確認
cd ~/tama/bedrock-server-1.21.114.1
ls -la bedrock_server
```

### WebSocketが接続しない
- ブラウザのコンソールを確認
- ファイアウォール設定を確認
- Cloudflare Tunnelの状態を確認

### リアルタイム更新されない
```bash
# サーバーログ確認
tail -f ~/tama/bedrock-server-1.21.114.1/logs/latest.log

# Node.jsサーバーログ確認
pm2 logs # (PM2使用時)
```

## 📊 監視内容

| 項目 | チェック方法 | 更新頻度 |
|------|-------------|---------|
| プロセス状態 | `ps aux \| grep bedrock_server` | 3秒 |
| Screen状態 | `screen -ls \| grep tama` | 3秒 |
| ポート状態 | `lsof -i :4096` | 3秒 |
| 稼働時間 | プロセスの起動時間 | 3秒 |

## 🎯 今後の改善案

- [ ] プレイヤーリストの実際の取得
- [ ] サーバーパフォーマンスグラフ
- [ ] 自動バックアップ機能
- [ ] 通知機能（Discord連携）
- [ ] モバイルアプリ対応

## 📝 ライセンス

MIT License

## 👤 作成者

Bedrock Server Admin Team
